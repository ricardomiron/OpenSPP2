### 19.0.3.0.0

- refactor: remove the Add Member birth-verification (model extension, apply override, CR verification banner, views and tests). The OP#871 Add Member redesign changed the flow to "add an existing member" and removed the create-new-individual fields it depended on. The reusable `dci_verification` utils and demo data remain. To be re-homed onto the new flow or retired — see #1133 (#242)

### 19.0.2.0.0

- Initial migration to OpenSPP2
