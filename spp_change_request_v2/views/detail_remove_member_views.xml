<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <!-- Remove Member Detail Form View -->
    <record id="spp_cr_detail_remove_member_form" model="ir.ui.view">
        <field name="name">spp.cr.detail.remove_member.form</field>
        <field name="model">spp.cr.detail.remove_member</field>
        <field name="arch" type="xml">
            <form
                string="Remove Member Details"
                readonly="not is_cr_manager or approval_state not in ('draft', 'revision')"
            >
                <header>
                    <field name="is_cr_manager" invisible="1" />
                    <button
                        name="action_next_documents"
                        string="Next: Upload Documents"
                        type="object"
                        class="btn-primary"
                        icon="fa-arrow-right"
                        invisible="approval_state not in ('draft', 'revision')"
                        groups="spp_change_request_v2.group_cr_manager"
                    />
                    <button
                        name="action_skip_to_review"
                        string="Review &amp; Submit"
                        type="object"
                        class="btn-success"
                        icon="fa-check-circle"
                        invisible="approval_state not in ('draft', 'revision')"
                        groups="spp_change_request_v2.group_cr_manager"
                    />
                    <button
                        name="action_save_and_go_to_list"
                        string="Save as Draft"
                        type="object"
                        class="btn-outline-secondary"
                        invisible="approval_state not in ('draft', 'revision')"
                        groups="spp_change_request_v2.group_cr_manager"
                    />
                    <field
                        name="stage"
                        widget="statusbar"
                        statusbar_visible="details,documents,review"
                    />
                </header>
                <sheet>
                    <group>
                        <group string="Member Selection">
                            <field name="available_individual_ids" invisible="1" />
                            <field
                                name="individual_id"
                                options="{'no_create': True}"
                                domain="[('id', 'in', available_individual_ids)]"
                                required="1"
                                readonly="not is_cr_manager or approval_state not in ('draft', 'revision')"
                            />
                            <field name="membership_id" invisible="1" force_save="1" />
                        </group>
                        <group string="Removal Details">
                            <field
                                name="end_reason"
                                readonly="not is_cr_manager or approval_state not in ('draft', 'revision')"
                            />
                        </group>
                    </group>
                    <group string="Additional Information">
                        <field
                            name="remarks"
                            placeholder="Enter any additional notes..."
                            readonly="not is_cr_manager or approval_state not in ('draft', 'revision')"
                        />
                    </group>
                </sheet>
            </form>
        </field>
    </record>
</odoo>
