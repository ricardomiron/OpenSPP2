<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <!-- Change Head of Household Detail Form View (OP#873 redesign) -->
    <record id="spp_cr_detail_change_hoh_form" model="ir.ui.view">
        <field name="name">spp.cr.detail.change_hoh.form</field>
        <field name="model">spp.cr.detail.change_hoh</field>
        <field name="arch" type="xml">
            <form
                string="Change Head of Household Details"
                readonly="not is_cr_manager or approval_state not in ('draft', 'revision')"
            >
                <header>
                    <field name="is_cr_manager" invisible="1" />
                    <button
                        name="action_next_documents"
                        string="Next: Upload Documents"
                        type="object"
                        class="btn-primary"
                        icon="fa-arrow-right"
                        invisible="approval_state not in ('draft', 'revision')"
                        groups="spp_change_request_v2.group_cr_manager"
                    />
                    <button
                        name="action_skip_to_review"
                        string="Review &amp; Submit"
                        type="object"
                        class="btn-success"
                        icon="fa-check-circle"
                        invisible="approval_state not in ('draft', 'revision')"
                        groups="spp_change_request_v2.group_cr_manager"
                    />
                    <button
                        name="action_save_and_go_to_list"
                        string="Save as Draft"
                        type="object"
                        class="btn-outline-secondary"
                        invisible="approval_state not in ('draft', 'revision')"
                        groups="spp_change_request_v2.group_cr_manager"
                    />
                    <field
                        name="stage"
                        widget="statusbar"
                        statusbar_visible="details,documents,review"
                    />
                </header>
                <sheet>
                    <group>
                        <group string="Household">
                            <field name="change_request_id" invisible="1" />
                            <field
                                name="current_head_id"
                                readonly="1"
                                options="{'no_open': True}"
                            />
                        </group>
                        <group string="Change Details">
                            <field
                                name="reason"
                                readonly="not is_cr_manager or approval_state not in ('draft', 'revision')"
                            />
                        </group>
                    </group>

                    <separator string="Members &amp; Roles" />
                    <div class="alert alert-info" role="alert">
                        <strong>Assign roles to the group members.</strong>
                        Set one member's <em>New Role</em> to <strong
                    >Head</strong> to designate
                        them as the new Head of Household. Each member's current role is shown for
                        reference.
                    </div>
                    <field name="member_line_ids" nolabel="1">
                        <list editable="bottom" create="0" delete="0">
                            <field
                                name="individual_id"
                                readonly="1"
                                options="{'no_open': True}"
                            />
                            <field
                                name="old_role_display"
                                string="Current Role"
                                readonly="1"
                            />
                            <field
                                name="new_role_id"
                                string="New Role"
                                options="{'no_create': True, 'no_open': True}"
                            />
                        </list>
                    </field>

                    <group string="Additional Information">
                        <field
                            name="remarks"
                            placeholder="Enter any additional notes..."
                            readonly="not is_cr_manager or approval_state not in ('draft', 'revision')"
                        />
                    </group>
                </sheet>
                <chatter />
            </form>
        </field>
    </record>
</odoo>
