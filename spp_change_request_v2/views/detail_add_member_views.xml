<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <!-- Add Member Detail Form View (OP#871 — search an existing member) -->
    <record id="spp_cr_detail_add_member_form" model="ir.ui.view">
        <field name="name">spp.cr.detail.add_member.form</field>
        <field name="model">spp.cr.detail.add_member</field>
        <field name="arch" type="xml">
            <form
                readonly="not is_cr_manager or approval_state not in ('draft', 'revision')"
            >
                <header>
                    <field name="is_cr_manager" invisible="1" />
                    <field name="type_requires_head" invisible="1" />
                    <field name="roles_available" invisible="1" />
                    <field name="individual_domain" invisible="1" />
                    <button
                        name="action_next_documents"
                        string="Next: Upload Documents"
                        type="object"
                        class="btn-primary"
                        icon="fa-arrow-right"
                        invisible="approval_state not in ('draft', 'revision')"
                        groups="spp_change_request_v2.group_cr_manager"
                    />
                    <button
                        name="action_skip_to_review"
                        string="Review &amp; Submit"
                        type="object"
                        class="btn-success"
                        icon="fa-check-circle"
                        invisible="approval_state not in ('draft', 'revision')"
                        groups="spp_change_request_v2.group_cr_manager"
                    />
                    <button
                        name="action_save_and_go_to_list"
                        string="Save as Draft"
                        type="object"
                        class="btn-outline-secondary"
                        invisible="approval_state not in ('draft', 'revision')"
                        groups="spp_change_request_v2.group_cr_manager"
                    />
                    <field
                        name="stage"
                        widget="statusbar"
                        statusbar_visible="details,documents,review"
                    />
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1>Add Member to Group</h1>
                    </div>

                    <!-- Review-stage hint above the proposed-changes table. -->
                    <div
                        class="alert alert-info"
                        role="alert"
                        invisible="stage != 'review'"
                    >
                        <strong
                        >The following individual is to be added to the group:</strong>
                    </div>

                    <group string="Target Group">
                        <field
                            name="registrant_id"
                            readonly="1"
                            options="{'no_open': True}"
                        />
                        <field
                            name="registrant_id_display"
                            string="Group ID(s)"
                            readonly="1"
                        />
                        <field name="registrant_registration_date" readonly="1" />
                        <field name="change_request_id" invisible="1" />
                    </group>

                    <group string="Member to Add">
                        <field
                            name="individual_id"
                            domain="individual_domain"
                            options="{'no_create': True, 'no_quick_create': True}"
                            placeholder="Search for an existing individual..."
                            required="1"
                        />
                        <field
                            name="membership_type_id"
                            string="Role"
                            options="{'no_create': True, 'no_open': True}"
                            domain="[('vocabulary_id.namespace_uri', '=', 'urn:openspp:vocab:group-membership-type')]"
                            required="type_requires_head"
                            invisible="not roles_available"
                        />
                    </group>

                    <separator string="Current members (read-only)" />
                    <field name="existing_membership_ids" nolabel="1" readonly="1">
                        <list create="0" edit="0" delete="0" no_open="1">
                            <field name="individual" />
                            <field name="membership_type_ids" widget="many2many_tags" />
                            <field
                                name="individual_registration_date"
                                string="Registration Date"
                            />
                            <field name="start_date" string="Joined" />
                        </list>
                    </field>
                </sheet>
                <chatter />
            </form>
        </field>
    </record>
</odoo>
